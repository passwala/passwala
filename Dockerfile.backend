FROM node:20-alpine
WORKDIR /app

COPY package.json package-lock.json ./
RUN npm ci --omit=dev

COPY server ./server
COPY database ./database
COPY scratch_query.cjs ./

ENV NODE_ENV=production
ENV PORT=3004

EXPOSE 3004
CMD ["node", "server/index.js"]
